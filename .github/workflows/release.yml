name: Release
on:
  workflow_call:
    inputs:
      dry-run:
        description: 'Compiles the app but not upload artifacts to distribution server'
        default: false
        required: false
        type: boolean
    secrets:
      MACOS_CSC_LINK:
        description: 'Certificate to sign dmg file'
        required: true
      MACOS_CSC_KEY_PASSWORD:
        description: 'Password to decrypt the certificate file'
        required: true
      MACOS_NOTARIZATION_APPLE_ID:
        description: 'Apple ID used for notarization'
        required: true
      MACOS_NOTARIZATION_TEAM_ID:
        description: 'Team ID used for notarization'
        required: true
      MACOS_NOTARIZATION_PWD:
        description: 'Password used for notarization'
        required: true
      ES_USERNAME:
        description: 'Username to sign exe file'
        required: true
      ES_PASSWORD:
        description: 'Password to sign the exe file'
        required: true
      WINDOWS_CREDENTIAL_ID_SIGNER:
        description: 'Certificate to sign the exe file'
        required: true
      ES_TOTP_SECRET:
        description: 'Password to decrypt the certificate file'
        required: true
      SEGMENT_API_KEY: 
        description: 'Segment API key'
        required: true
      SENTRY_AUTH_TOKEN:
        description: 'Sentry auth token'
        required: true
      SENTRY_DSN:
        description: 'Sentry DSN to perform the requests'
        required: true
      TAURI_SIGNING_PRIVATE_KEY:
        description: 'Private key used to sign the Tauri updater'
        required: true
      TAURI_SIGNING_PRIVATE_KEY_PASSWORD:
        description: 'Password to unlock the private key for Tauri updater'
        required: true
      EXPLORER_TEAM_AWS_ACCESS_KEY_ID:
        description: 'AWS access key to upload artifacts to S3'
        required: true
      EXPLORER_TEAM_AWS_SECRET_ACCESS_KEY:
        description: 'AWS secret access key to upload artifacts to S3'
        required: true
      EXPLORER_TEAM_AWS_DEFAULT_REGION:
        description: 'AWS region where the S3 bucket is located'
        required: true
      EXPLORER_TEAM_S3_BUCKET:
        description: 'AWS S3 bucket name to upload artifacts'
        required: true
      DEV_EXPLORER_TEAM_AWS_ACCESS_KEY_ID:
        description: 'DEV AWS access key to upload artifacts to S3'
        required: true
      DEV_EXPLORER_TEAM_AWS_SECRET_ACCESS_KEY:
        description: 'DEV AWS secret access key to upload artifacts to S3'
        required: true
      DEV_EXPLORER_TEAM_AWS_DEFAULT_REGION:
        description: 'DEV AWS region where the S3 bucket is located'
        required: true
      DEV_EXPLORER_TEAM_S3_BUCKET:
        description: 'DEV AWS S3 bucket name to upload artifacts'
        required: true

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: true

defaults:
  run:
    shell: 'bash'

env:
  VITE_AWS_S3_BUCKET_PUBLIC_URL: ${{ vars.EXPLORER_TEAM_S3_BUCKET_PUBLIC_URL }}
  PROJECT_PATH: src-tauri

jobs:
  draft_release:
    permissions:
      contents: write # Allows this job to create releases

    strategy:
      fail-fast: true
      matrix:
        include:
          # - os: macos-latest
          #   bundles: [dmg, macos]
          - os: windows-latest
            bundles: [nsis]

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true

      - name: Diagnose sign.ps1 script
        if: ${{ matrix.os == 'windows-latest' }}
        shell: pwsh
        run: |
          $script = Resolve-Path "src-tauri\scripts\sign.ps1"
          Write-Host "Script path: $script"

          # Check extension
          if ($script -notmatch '\.ps1$') {
            Write-Warning "File does not end with .ps1 extension"
          } else {
            Write-Host "✅ .ps1 extension confirmed"
          }

          # Check file attributes
          $file = Get-Item $script
          Write-Host "File attributes: $($file.Attributes)"
          if (-not $file.Exists) {
            Write-Error "File does not exist"
          }

          # Check execution policy
          Write-Host "ExecutionPolicy: $(Get-ExecutionPolicy -List | Out-String)"

          # Check for rogue characters
          $bytes = [System.IO.File]::ReadAllBytes($script)
          if ($bytes[0] -eq 0xEF -and $bytes[1] -eq 0xBB -and $bytes[2] -eq 0xBF) {
            Write-Host "✅ UTF-8 BOM detected"
          } else {
            Write-Warning "⚠️ No UTF-8 BOM detected"
          }

          # Search for non-printable characters (e.g., zero-width space)
          $raw = Get-Content $script -Raw
          $zwsp = $raw -match "\u200B"
          if ($zwsp) {
            Write-Warning "⚠️ Zero-width space detected in script"
          } else {
            Write-Host "✅ No zero-width space found"
          }

          # Output first few lines
          Write-Host "Preview of script (first 10 lines):"
          Get-Content $script -TotalCount 10 | ForEach-Object { Write-Host $_ }

      - name: Download and test CodeSignTool
        if: ${{ matrix.os == 'windows-latest' }}
        shell: pwsh
        run: |
          $version = "v1.3.0"
          $url = "https://github.com/SSLcom/CodeSignTool/releases/download/$version/CodeSignTool-$version-windows.zip"
          $zipPath = "C:\CodeSignTool.zip"
          $extractPath = "C:\CodeSignTool"

          Invoke-WebRequest -Uri $url -OutFile $zipPath
          Expand-Archive -Path $zipPath -DestinationPath $extractPath -Force

          $jarPath = Get-ChildItem -Path $extractPath -Filter "code_sign_tool-*.jar" -Recurse | Select-Object -First 1
          if (-not $jarPath) {
            Write-Error "CodeSignTool JAR not found"
            exit 1
          }

          echo "CODESIGN_JAR=$($jarPath.FullName)" | Out-File -FilePath $env:GITHUB_ENV -Append
          Write-Host "CodeSignTool JAR set to: $($jarPath.FullName)"

          $testFile = "$env:WINDIR\System32\WindowsPowerShell\v1.0\powershell.exe"
          Write-Host "Testing sign.ps1 with: $testFile"

          $signScript = (Resolve-Path "src-tauri\scripts\sign.ps1").ToString()
          if (-not (Test-Path $signScript)) {
            Write-Error "sign.ps1 not found at $signScript"
            exit 1
          }

          Write-Host "----- sign.ps1 Preview -----"
          Get-Content $signScript | ForEach-Object { Write-Host $_ }
          Write-Host "-----------------------------"

          $rawText = [System.IO.File]::ReadAllText($signScript) -replace "`r?`n", "`r`n"
          $utf8bom = New-Object System.Text.UTF8Encoding($true)
          [System.IO.File]::WriteAllText($signScript, $rawText, $utf8bom)

          $bytes = [System.IO.File]::ReadAllBytes($signScript)
          if ($bytes[0] -eq 0xEF -and $bytes[1] -eq 0xBB -and $bytes[2] -eq 0xBF) {
            Write-Host "UTF-8 BOM confirmed"
          } else {
            Write-Warning "No BOM found after normalization"
          }

          Write-Host "Invoking sign.ps1 via Start-Process..."
          Start-Process pwsh -Wait -NoNewWindow -ArgumentList "-NoProfile", "-ExecutionPolicy", "Bypass", "-File", $signScript, $testFile

      - uses: swatinem/rust-cache@v2
        with:
          workspaces: src-tauri
          cache-targets: true
          cache-provider: github

      - uses: actions/setup-node@v4
        with:
          node-version: lts/*
          cache: 'npm'

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.platform == 'macos-latest' && 'aarch64-apple-darwin,x86_64-apple-darwin' || '' }}

      - run: npm ci
        env:
          PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD: 1

      - run: npm run build
        env:
          MODE: production
          DRY_RUN: ${{ inputs.dry-run }}
          VITE_SEGMENT_API_KEY: ${{ secrets.SEGMENT_API_KEY }}
          SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
          VITE_SENTRY_DSN: ${{ secrets.SENTRY_DSN }}

      - name: Generate Release Version
        id: version
        uses: paulhatch/semantic-version@v5.4.0
        with:
          tag_prefix: ""
          version_format: ${major}.${minor}.${patch}
          major_pattern: "/^(major|breaking).+/"
          minor_pattern: "/^(minor|feat).+/"
          bump_each_commit: false
          bump_each_commit_patch_pattern: "/^(patch|fix).+/"
          search_commit_body: true
          user_format_type: "json"

      - name: Import Apple Developer certificate
        if: ${{ matrix.os == 'macos-latest' && inputs.dry-run == false }}
        env:
          APPLE_CERTIFICATE: ${{ secrets.MACOS_CSC_LINK }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.MACOS_CSC_KEY_PASSWORD }}
          KEYCHAIN_PASSWORD: ${{ secrets.MACOS_CSC_KEY_PASSWORD }}
        run: |
          echo $APPLE_CERTIFICATE | base64 --decode > certificate.p12
          security create-keychain -p "$KEYCHAIN_PASSWORD" build.keychain
          security default-keychain -s build.keychain
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" build.keychain
          security set-keychain-settings -t 3600 -u build.keychain
          security import certificate.p12 -k build.keychain -P "$APPLE_CERTIFICATE_PASSWORD" -T /usr/bin/codesign
          security set-key-partition-list -S apple-tool:,apple:,codesign: -s -k "$KEYCHAIN_PASSWORD" build.keychain
          security find-identity -v -p codesigning build.keychain

      - name: Verify Apple certificate
        if: ${{ matrix.os == 'macos-latest' && inputs.dry-run == false }}
        run: |
          CERT_INFO=$(security find-identity -v -p codesigning build.keychain | grep "Developer ID Application")
          CERT_ID=$(echo "$CERT_INFO" | awk -F'"' '{print $2}')
          echo "CERT_ID=$CERT_ID" >> $GITHUB_ENV
          echo "Certificate imported."

      - name: Build Release
        if: ${{ inputs.dry-run == false }}
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          APPLE_ID: ${{ secrets.MACOS_NOTARIZATION_APPLE_ID }}
          APPLE_PASSWORD: ${{ secrets.MACOS_NOTARIZATION_PWD }}
          APPLE_TEAM_ID: ${{ secrets.MACOS_NOTARIZATION_TEAM_ID }}
          APPLE_CERTIFICATE: ${{ secrets.MACOS_CSC_LINK }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.MACOS_CSC_KEY_PASSWORD }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
          SEGMENT_API_KEY: ${{ secrets.SEGMENT_API_KEY }}
          SENTRY_DSN: ${{ secrets.SENTRY_DSN }}
          ES_USERNAME: ${{ secrets.ES_USERNAME }}
          ES_PASSWORD: ${{ secrets.ES_PASSWORD }}
          WINDOWS_CREDENTIAL_ID_SIGNER: ${{ secrets.WINDOWS_CREDENTIAL_ID_SIGNER }}
          ES_TOTP_SECRET: ${{ secrets.ES_TOTP_SECRET }}
        with:
          tagName: app-v__VERSION__
          releaseName: "App v__VERSION__"
          releaseBody: "See the assets to download this version and install."
          releaseDraft: true
          prerelease: false
          args: ${{ secrets.args }}
          projectPath: ${{ env.PROJECT_PATH }}

      - name: Build Dry-Run
        if: ${{ inputs.dry-run == true }}
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          APPLE_ID: ${{ secrets.MACOS_NOTARIZATION_APPLE_ID }}
          APPLE_PASSWORD: ${{ secrets.MACOS_NOTARIZATION_PWD }}
          APPLE_TEAM_ID: ${{ secrets.MACOS_NOTARIZATION_TEAM_ID }}
          APPLE_CERTIFICATE: ${{ secrets.MACOS_CSC_LINK }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.MACOS_CSC_KEY_PASSWORD }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
          SEGMENT_API_KEY: ${{ secrets.SEGMENT_API_KEY }}
          SENTRY_DSN: ${{ secrets.SENTRY_DSN }}
        with:
          args: ${{ secrets.args }}
          projectPath: ${{ env.PROJECT_PATH }}

      - name: List Tauri build outputs
        if: ${{ matrix.os == 'windows-latest' && inputs.dry-run == false }}
        id: list-bundles
        run: |
          ls -R src-tauri/target/release/bundle
          EXE=$(find src-tauri/target/release/bundle/nsis -name '*.exe' | head -n1)
          echo "file_path=$EXE" >> $GITHUB_OUTPUT
          echo "File path: $EXE"

      # - name: Sign Windows artifact
      #   if: ${{ matrix.os == 'windows-latest' && inputs.dry-run == false }}
      #   uses: sslcom/esigner-codesign@main
      #   with:
      #     command: sign
      #     username: ${{ secrets.ES_USERNAME }}
      #     password: "${{ secrets.ES_PASSWORD }}"
      #     credential_id: ${{ secrets.WINDOWS_CREDENTIAL_ID_SIGNER }}
      #     totp_secret: ${{ secrets.ES_TOTP_SECRET }}
      #     file_path: ${{ steps.list-bundles.outputs.file_path }}
      #     malware_block: false
      #     override: true
      #     signing_method: v2

      - name: Verify Windows signature
        if: ${{ matrix.os == 'windows-latest' && inputs.dry-run == false }}
        run: |
          SIGNED_EXE="${{ steps.list-bundles.outputs.file_path }}"
          echo "Built artifact: $SIGNED_EXE"
          if [[ -f "$SIGNED_EXE" ]]; then
            echo "Checking signature..."
            powershell.exe -Command "Get-AuthenticodeSignature '$SIGNED_EXE'"
            SIGNATURE_STATUS=$(powershell.exe -Command "(Get-AuthenticodeSignature '$SIGNED_EXE').Status")
            echo "Signature status: $SIGNATURE_STATUS"
            if [[ "$SIGNATURE_STATUS" != "Valid" ]]; then
              echo "❌ EXE is not signed or signature is invalid"
              exit 1
            fi
          else
            echo "❌ Signed EXE not found"
            exit 1
          fi

      - name: Upload Release artifacts to S3
        if: ${{ inputs.dry-run == false }}
        uses: ./.github/actions/upload-to-s3
        with:
          aws-access-key-id: ${{ secrets.EXPLORER_TEAM_AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.EXPLORER_TEAM_AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.EXPLORER_TEAM_AWS_DEFAULT_REGION }}
          s3-bucket: ${{ secrets.EXPLORER_TEAM_S3_BUCKET }}
          target-dir: ${{ github.event.repository.name }}

      - name: Upload Dry-Run artifacts to S3
        if: ${{ inputs.dry-run == true }}
        uses: ./.github/actions/upload-to-s3
        with:
          aws-access-key-id: ${{ secrets.DEV_EXPLORER_TEAM_AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.DEV_EXPLORER_TEAM_AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.DEV_EXPLORER_TEAM_AWS_DEFAULT_REGION }}
          s3-bucket: ${{ secrets.DEV_EXPLORER_TEAM_S3_BUCKET }}
          target-dir: dry-run-${{ github.event.repository.name || 'unknown-repo' }}/pr-${{ github.event.pull_request.number || 'na' }}/run-${{ github.run_number || '0' }}-${{ github.run_id || 'id' }}
